config:
  target: 'http://localhost:8000'
  phases:
    - duration: 60
      arrivalRate: 10
  defaults:
    headers:
      Accept: 'application/json'

scenarios:
  - flow:
    - get:
      url: '/intentions/read'